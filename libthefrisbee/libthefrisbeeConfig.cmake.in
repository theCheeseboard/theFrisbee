set(libthefrisbee_VERSION 6.0.0)

@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/libthefrisbeeTargets.cmake")

file(GLOB EXTRA_FUNCTIONS "${_DIR}/cmake-scripts/*.cmake")
foreach(f ${EXTRA_FUNCTIONS})
include(${f})
endforeach()

check_required_components(libthefrisbee)